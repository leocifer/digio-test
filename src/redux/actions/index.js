export const login = () => ({
	type: 'LOGIN',
})

export const logout = () => ({
	type: 'LOGOUT',
})

export const verify = () => ({
	type: 'VERIFY',
})

export const LoginActions = {
	LOGIN: 'LOGIN',
	LOGOUT: 'LOGOUT',
	VERIFY: 'VERIFY',
}
